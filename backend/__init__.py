"""
Backend package for the Dark SaaS Landing Page.

This makes the `backend` directory importable as a Python package, so you can run:

    uvicorn backend.main:app --reload

from the project root.
"""

